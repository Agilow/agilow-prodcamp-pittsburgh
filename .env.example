# Frontend (Vite) env. Copy to .env.local for local overrides, or set in the
# Vercel project dashboard for production.
#
# Local dev: leave this UNSET — the Vite dev proxy forwards /api/* to :8787.
# Production: set to your Render backend origin (no trailing slash).
VITE_API_URL=
